<?php

namespace App\Cache;

use App\Repositories\ConfigTypeApplicationRepository;
use Illuminate\Database\Eloquent\Model;

class ConfigTypeApplicationCache extends BaseCache {

    /**
     * __construct
     *
     * @return void
     */
    public function __construct(ConfigTypeApplicationRepository $typeApplicationRepository) {
        parent::__construct($typeApplicationRepository);
    }

    /**
     * all
     *
     * @param  mixed $request
     * @return void
     */
    public function all($request)
    {
        return $this->cache::remember($this->key, $this->ttl, function () use ($request) {
            return $this->repository->all($request);
        });
    }

    /**
     * find
     *
     * @param  mixed $id
     * @return void
     */
    public function find ($id) {
        return $this->cache::remember($this->key, $this->ttl, function () use ($id) {
            return $this->repository->find($id);
        });
    }

    /**
     * save
     *
     * @param  mixed $model
     * @return void
     */
    public function save(Model $model)
    {
        $this->forgetCache('configTypeApplications');
        return $this->repository->save($model);
    }

    /**
     * destroy
     *
     * @return void
     */
    public function destroy (Model $model) {
        $this->forgetCache('configTypeApplications');
        return $this->repository->destroy($model);
    }
}
